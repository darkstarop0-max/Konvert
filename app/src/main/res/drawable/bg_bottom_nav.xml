<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background with subtle blur effect -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/nav_background_blur" />
            <corners android:radius="20dp" />
        </shape>
    </item>
    
    <!-- Subtle border -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="0.5dp"
                android:color="@color/nav_border" />
            <corners android:radius="20dp" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
    
    <!-- Subtle inner glow effect -->
    <item
        android:top="1dp"
        android:left="1dp"
        android:right="1dp"
        android:bottom="1dp">
        <shape android:shape="rectangle">
            <stroke
                android:width="0.5dp"
                android:color="@color/nav_inner_glow" />
            <corners android:radius="19dp" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</layer-list>
